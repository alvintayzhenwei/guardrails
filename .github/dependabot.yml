version: 2
updates:
  # This repo vendors no runtime dependencies; the only supply chain it has is
  # the set of GitHub Actions its own workflows pull in.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: 'ci'
    labels:
      - dependencies
